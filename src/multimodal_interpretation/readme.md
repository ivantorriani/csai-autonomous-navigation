This Week’s Tasks: Pick one of the best multimodal models from your list and produce an script which takes an image (such as an jpg file) as input,  parses it through that model, and outputs an JSON entry that mimics the following structure. For example, if this image was the input:

```json
"traversability": false,
"justification": "The image appears to contain someone dressed up as"
									"a stoplight or traffic cone."

```